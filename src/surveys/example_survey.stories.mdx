import { Canvas, Meta, Story } from '@storybook/addon-docs';
import SurveyWrapper from '../SurveyWrapper';
export const Template = (args) => <SurveyWrapper {...args} />;
import surveyJson from "./example_survey.json"
<Meta title="MDX/ExampleSurvey" component={SurveyWrapper} />

# myfunheading

<Story 
  name="Example"
  args={{surveyJson: surveyJson}}
>
  {Template.bind({})}
</Story>
